<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to post the live API-activity notification on API 33+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <activity
            android:name=".ui.RestApiDebuggerActivity"
            android:exported="false"
            android:label="REST API Debugger"
            android:theme="@style/Theme.RestApiDebugger" />
    </application>

</manifest>
