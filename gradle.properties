org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Toggles the REST API debugger (rest-api-debugger module) in debug builds.
# Override on the command line with -PdebugRestApi=false. Release builds
# always hard-disable it regardless of this flag (see app/build.gradle.kts).
debugRestApi=true
